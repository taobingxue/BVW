using UnityEngine;
using System.Collections;

public class RemoveLastScore : MonoBehaviour {

	// Use this for initialization
	void Start () {
		PlayerPrefs.SetInt("score8", 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
